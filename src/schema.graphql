type Query {
  workspaces: [Workspace]
  workspace(id: ID!): Workspace
}

type Mutation {
  openTmux(name: String!, force: Boolean): Boolean
}

# type Subscription {
#   # feedSubscription: Post
# }

type WorkspaceFolder {
  name: String
  path: String!
}
type Workspace {
  id: String!
  folders: [WorkspaceFolder]
}
